# Resolved environment file from op inject — never commit
.env

# Build / handoff docs — maintainer-internal, not for public plugin
BUILD_HANDOFF.md
debug-transition-context.md
debugging-setup-briefing.md

# Maintainer navigation aid — auto-loaded by Claude Code, gitignored to stay maintainer-only
CLAUDE.local.md

# macOS
.DS_Store

# Temporal MCP offload cache — created at runtime in the project dir, 24h TTL, never commit
.payrails-temporal-cache/

# Playwright MCP runtime output dir — created when the browser tool runs, never commit
.playwright-mcp/

# Maintainer-local working files (not for the public plugin)
# grafana.md*backup covers both: grafana.md.backup (pre-gcx self-hosted-MCP version)
# and grafana.md.gcx-backup (the gcx-CLI version, kept in case we revert from the cloud MCP)
grafana.md*backup
payrails-backend-github-setup.md
